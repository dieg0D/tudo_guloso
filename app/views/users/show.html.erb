<%= render partial: './common/header' %>
<div class="div-body container">
  <h3>Perfil de <%= @user.name %></h3>
  <%= @user.email %> <% if current_user == @user %>
    <ul>
      <li><%= link_to "Edit", edit_user_path(current_user) %></li>
      <li><%= link_to "Delete", user_path(@user), method: :delete, data: { confirm: 'Você tem certeza?' }%></li>
    </ul>
  <%end%>
</div>

<%= render partial: './common/footer' %>